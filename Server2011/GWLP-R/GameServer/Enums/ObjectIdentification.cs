namespace GameServer.Enums
{
        public enum Items
        {
                ItemID,
        }

        public enum Maps
        {
                MapID,
                GameMapID,
                GameFileID
        }

        public enum Chars
        {
                CharID,
                AccID,
                NetID,
                LocalID,
                AgentID,
                Name
        }

        public enum Clients
        {
                NetID,
                AccID,
                CharID
        }
}
